This branch contains installer improvements:

- Robust multi-distro package installer helper
- Termux-friendly behavior (no sudo)
- Auto-install Oh My Zsh, Powerlevel10k, and common plugins
- Copies `p10k` and `fastfetch` configs from `configs/`

See `CHANGELOG.md` for full details.
